[project]
name = "homelab-xcp"
version = "2.0.0"
description = "Ansible playbooks to manage configuration and create VMs for Erhhung's XCP-ng cluster at home."
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["ansible-core<=2.16.20", "ansible-lint>=25.8.2"]
