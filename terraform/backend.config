# assumes ~/.aws/config and ~/.aws/credentials
# have already defined "personal" profile with
# read/write access to the tfstate S3 bucket

profile = "personal"
region = "us-west-2"
bucket = "homelab-infra-tfstate"
key = "xcp/k8s.tfstate"
